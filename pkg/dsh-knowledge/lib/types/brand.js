/**
 * Brand an implementation-minted document identity.
 * @param id - opaque document identity.
 * @returns the same string, branded; no validation is performed.
 */
export function KnowledgeId(id) {
    return id;
}
//# sourceMappingURL=brand.js.map